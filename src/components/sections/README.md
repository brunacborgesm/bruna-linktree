# Linktree Components

This directory manages the UI blocks for the links aggregator.

### Key Components:
- **LinksPage**: The main container that handles layout, portrait display, and the list of social items.
- **LinkItems**: Individual, interactive link bars with hover effects and brand icons.

The structure is designed to be extremely lightweight to ensure instant load times when accessed from social media profiles.
